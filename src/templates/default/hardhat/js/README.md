# {{projectName}}

Hardhat JavaScript project scaffolded by Compose CLI. Hardhat is installed directly from npm; this project follows the [Hardhat 3 project structure](https://hardhat.org/docs/getting-started#project-structure).

- `contracts/` – Solidity contracts and `.t.sol` tests
- `test/` – TypeScript/JavaScript tests (optional)
- `scripts/` – deployment and automation scripts
- `ignition/modules/` – [Hardhat Ignition](https://hardhat.org/ignition) deployment modules (optional)

## Next steps

1. `cd {{projectName}}` (if you haven’t already)
2. Run `npx hardhat compile` then `npx hardhat test`
